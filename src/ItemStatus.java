// This code is defining an enumeration type called `ItemStatus` in Java. It has three possible values:
// `onSale`, `outOfStock`, and `available`. This enum can be used to represent the status of an item in
// a program.
public enum ItemStatus {
    onSale,
    outOfStock,
    available
}
